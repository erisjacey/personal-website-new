/* eslint-disable no-multi-str */
import React from 'react';
import { Container, Typography } from '@mui/material';

const Work = () => (
  <Container id="work">
    <Typography
      variant="h3"
    >
      Work Page
    </Typography>
  </Container>
);

export default Work;
