/* eslint-disable no-multi-str */
import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => (
  <Container>
    <Typography
      variant="h3"
    >
      Home Page
    </Typography>
  </Container>
);

export default Home;
