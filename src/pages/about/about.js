import React from 'react';
import { Container, Typography } from '@mui/material';

const About = () => (
  <Container id="about">
    <Typography
      variant="h3"
    >
      About Page
    </Typography>
  </Container>
);

export default About;
