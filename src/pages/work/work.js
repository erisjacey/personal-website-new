/* eslint-disable no-multi-str */
import React from 'react';
import { Container, Typography } from '@mui/material';

const Work = () => (
  <Container>
    <Typography
      variant="h3"
    >
      Work Page
    </Typography>
  </Container>
);

export default Work;
